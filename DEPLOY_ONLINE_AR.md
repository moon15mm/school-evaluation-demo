# رفع نسخة عرض على الإنترنت

هذه التعليمات مخصصة لرفع نسخة تسويقية ببيانات افتراضية، وليست نسخة تشغيل رسمية لمدرسة حقيقية.

## حساب العرض الافتراضي

- العرض فقط: `demo` / `demo123`

حساب `demo` مناسب للعرض التسويقي لأنه لا يملك صلاحية تعديل البيانات.

عند بناء حزمة الإنترنت، يتم إنشاء حساب مدير عشوائي في ملف خاص خارج ملف النشر. لا تضع بيانات المدير داخل صفحة عامة أو مستودع عام.

## النشر على Render

1. ارفع المشروع إلى مستودع GitHub خاص أو عام.
2. افتح Render واختر `New Web Service`.
3. اربط المستودع.
4. سيقرأ Render ملف `render.yaml` تلقائيًا.
5. تأكد من وجود المتغير:
   - `DEMO_MODE=true`
6. بعد اكتمال النشر، افتح رابط الخدمة وسجل الدخول بحساب `demo`.

## النشر على Railway

1. ارفع المشروع إلى GitHub.
2. افتح Railway وأنشئ مشروعًا جديدًا من GitHub Repo.
3. أضف متغير البيئة:
   - `DEMO_MODE=true`
4. اجعل أمر التشغيل:
   - `uvicorn app.main:app --host 0.0.0.0 --port $PORT`

## ماذا يفعل وضع Demo؟

عند تشغيل التطبيق والذاكرة فارغة، ينشئ النظام تلقائيًا:

- تقرير زيارة سابق افتراضي.
- تحليل شامل ومدخلات وضع حالي.
- خطة تحسين ومهام.
- شواهد تجريبية مصنفة داخل مجلدات.
- قصص أثر للملف الذهبي.
- جلسة وكلاء وتوصيات وتحذيرات.

## تنبيه مهم

منصات النشر المجانية قد تمسح الملفات عند إعادة التشغيل أو بعد فترة خمول. هذا مناسب للعرض التسويقي، لكنه غير مناسب لحفظ بيانات مدرسة حقيقية على المدى الطويل دون قاعدة بيانات أو تخزين دائم.
